# Notebooks

Scratch research notebooks. Anything that becomes a finding must be re-expressed as a named experiment in `config/experiments/` so it runs through the same point-in-time engine, verdict rules and provenance log.
